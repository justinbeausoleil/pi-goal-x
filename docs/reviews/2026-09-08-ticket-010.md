# Ticket010 provider recovery review

Runtime candidate: `e819b8d5e7734d078da2c6f86f213b4d7021e5cf`.
Fixed cumulative base: `eb09152`. G5/G12; native S1.
Both independent axes are clear; ticket qualification passes.

| Required outcome | Observable evidence |
| --- | --- |
| Recovery ownership | Actual Pi immediate retry executes one recovered write without extension backoff. Controlled extension timers assert native host idle. Three successful overflow compactions retain exact checkpoint counts; no duplicate goal continuation. |
| Error outcomes | Transient503 and transport abort without a user signal recover. Nontransient401 yields. Actual failed/cancelled threshold and overflow summaries preserve pending tasks and cannot dispatch the forbidden write. |
| Finite and unbounded policy | Cap3 schedules5000/10000/10000ms then reports exhaustion; successful work resets the next delay to5000ms. Explicit maxAttempts0 samples four capped-delay schedules, then cancels. Production policy is unchanged. |
| Stops and stale work | Pause, unfocus, focus change, reopen and new session cancel old backoff. Host-delivered stale custom messages cannot write; a nextTurn historical attachment preserves fresh user work. Reopen may start one newly owned checkpoint under D5. |
| New authorization during an old summary | Public pause/resume and replacement each retain their authorized write after the old summary fails. Cleanup rechecks the original focus generation, rather than erasing a successor's handoff. |
| No-progress preservation | Nine native text/inspection/work/clarification controls retain ticket009's yield and soft-guidance behavior. Failed requests never complete the public task. |

Evidence: `~/Data/pi-goal-x/reliability/010/review-checks.json` records exact
commands and candidate identities. The milestone log retains native reds,
fixture corrections and review repairs. Controlled timers change test elapsed
time only; requested delay values and the native lifecycle remain observable.

Spec independently passes8 supplied initial cases, reproduces the ownership
race, then passes7 repaired cases including its original reproduction
(`review-e819b8d-native.log`). Standards passes3 initial and2 final native cases
(`review-e819b8d-standards-pinned-checks.json`). A reviewer environment mistake
and its loader failures are retained in that manifest. Both axes report zero
remaining material findings.

Frozen d29cb5a passes1313/1313 full tests and953 discovery/selfchecks. Final
e819b8d passes6 ownership/compaction regressions, type/lint,53 unchanged B2/B7
timing limits and dry pack (65 files,267984 bytes). Its full suite passes1315/1315
across48 suites and76 files in238833ms, with zero failures/skips
(`ownership-full.log`). Historical NAF and
ranking5 pass; unchanged dependency and payload checks retain009 evidence.
No live selection changed. Packed installation, compatibility and real-model
acceptance remain012–014.
