# Ticket 009 blocker and Oracle review

Runtime candidate: `c9b44eee536e180320c0e4c7b1a359058eed3d14`.
Fixed cumulative base: `5500578`. G2/G12; native S1/S2.
Standards and Spec reviews are clear; ticket qualification passes.

| Required outcome | Observable evidence |
| --- | --- |
| Pause and blocked remain distinct | Public agent pause records reason/action; blocked records its concrete reason and requires user action. Actual compaction preserves guidance and task state. Three-consecutive-blocker recurrence remains delivered model policy, without a new runtime counter. |
| Optional Oracle outcomes | Actual local child transport covers actionable, needs_human, insufficient_context, malformed/invalid-index, provider401, abort, missing configuration and disabled Oracle. Two configured failed consultations allow the subsequent capped block without another request. |
| Durable advice and meaningful follow-up | Three real summaries omit advice, then actual host reopen restores it from the ledger. get_goal, ls and plain/quoted/escaped echo cannot discharge it. Actual write permits blocking and persists one follow-up event. A→B→A reuse selects A durably, including across reopen. |
| Consultation isolation and cancellation | Actual child payloads contain only read-only consultation tools and submit_goal_oracle_advice. Project-resource opt-in is verified by a sentinel. Stop/focus/session controls abort transport; failures remain diagnostic without a stale successful result. |
| Bounded idle continuation | Native text, repeated get_goal, echo and ls yield; real write, redirection and command substitution earn continuation. Soft inspection guidance remains present. An active goal asks clarification and resumes from the user's reply or explicit resume. Existing stall/compaction checks remain. |

Private evidence: `~/Data/pi-goal-x/reliability/009/`.
`review-checks.json` records exact commands, candidate identities and results.
The milestone log retains red cases and fixture corrections.

Spec independently passes9 initial native cases,2 fingerprint/variable repairs,
then5 final checks including its original quoted-echo repro
(`review-c9b44ee-native.log`). Standards independently passes6 initial native
checks and the same-response write/block probe,2 repair checks, then3 final
quoted/substitution checks (`review-c9b44ee-standards-checks.json`). Both axes
report zero remaining material findings; neither substitutes for qualification.

Frozen04ec071 passes1295/1295 full tests and953/953 discovery/selfchecks.
Final c9b44ee passes type checking,53 unchanged B2/B7 timing limits and dry pack
(65 files,267656 bytes). Its full suite passes1298/1298 across48 suites and76
files in231046ms, with no failures/skips (`echo-final-full.log`). Lint's unnecessary test
quote escape is corrected with an equivalent string; lint and its native case
pass afterward. Payload checks at956d4aa pass28 context fixtures and7 actual SDK
requests; historical NAF, ranking5 and production audit (zero findings) pass.
No dependencies changed. Shell work classification is deliberately bounded;
it does not establish the semantic usefulness of arbitrary shell programs.
Installed artifact compatibility, six Qwen runs and adoption remain012–014.
