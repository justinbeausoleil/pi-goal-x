# Ticket 002 independent review

Scope: ticket 002, spec G2/G4, plan D1/S1 and lifecycle preservation.
Initial fixed diff: `git diff c3a87a6...6a981769`.
Final follow-up: `git diff c3a87a6...32e8f188`.
Separate read-only Standards and Spec reviewers examined the cumulative slice.

## Standards

Initial documented-standard breach: architecture documentation no longer
described context delivery. Updated the existing architecture document with
aggregate bounds, stopped-state/stall projection, and Oracle persistence.

Initial smell judgment: Feature Envy in the context handler's full-history
Oracle lookup. Moved pending-advice retrieval into the existing ledger index;
legacy derived checkpoints rebuild from the authoritative ledger when needed.
Public paged history still returns the complete stored advice.

The reviewer also reproduced oversized malformed checkpoints. Pi catches
context-hook exceptions and may send the original messages, so throwing at the
cap did not enforce it. Every goal checkpoint is now normalized, including
missing/malformed IDs; oversized identities are rejected before projection.
Two real-host regressions preserve bounded stop guidance and prevent work.

Final Standards review at 32e8f188: all findings resolved; no remaining
documented-standard breaches or smell findings in this slice. Reviewer
inspected repairs and regression assertions; root qualification supplies the
full check results below.

## Spec

Initial review reproduced two defects:

- Stopped projections omitted a separately stored goal verification contract.
  Shared stopped-state context now includes its excerpt/retrieval locator.
  The native fixture uses a separate contract even with a short objective.
- Blocked and budget-limited projections omitted outstanding auditor rejection.
  Shared rejection guidance now survives all applicable stopped states and
  compaction without restarting autonomous execution.

Final Spec review at 32e8f188: both findings resolved, no remaining findings
within ticket 002. Reviewer independently reran five native cases: blocked
with audit, budget-limited with audit, long fields with Oracle/audit, oversized
malformed content, and oversized marker ID. All passed.

## Verification

- `npm run check`, `npm run lint`: passed at 32e8f188.
- `npm run test:lifecycle`: 26 actual-loader/session/provider cases passed.
- `npm run test:all`: 988/988 passed across 76 discovered files.
- `npm run test:selfcheck`: discovery checks and 916 unit tests passed.
- `npm run context:gate`: 24 fixtures, aggregate cap and native-summary
  counting probes passed. Documented baseline changes retain semantic counts.
- `npm run context:provider-check`: six actual SDK payloads passed.
- `npm run bench:gate:naf`, `npm pack --dry-run`,
  `npm audit --omit=dev`: passed; production audit reports zero vulnerabilities.

Exact commands/results and red/green logs are under
`~/Data/pi-goal-x/reliability/002/`, indexed by `review-checks.json`.
The context checks ran at 5af76d98; 32e8f188 only materializes a derived
checkpoint in a migration test. A prior concurrent context/provider run
collided on their shared temporary directory; failed logs are retained and
both tools passed sequentially. Other retained failures include threshold
fixture cut-point errors and the migration fixture's missing checkpoint.

The original fe430b25 source loses off-preview t40 after real compaction;
current manual/threshold/overflow cases preserve t40/t41/t42 and three completed
transitions through three compactions apiece. Threshold compacts inside one
run; overflow has one host recovery and no duplicate extension continuation.
Native Oracle/auditor children, six lifecycle states, one-shot stall guidance,
long fields, persisted-message pairing, and unrelated context are checked.

This slice proves the bounded small-plan lifecycle seam. Ticket 004 owns
200-node compaction/detail retrieval; 009 owns the full Oracle lifecycle;
011/012 own the observed global auditor-settings defect. Node compatibility,
installed package qualification, and all real-Qwen gates remain pending.
