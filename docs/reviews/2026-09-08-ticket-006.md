# Ticket 006 independent review and evidence

Scope: G6/G13 session ownership, legacy records and recovery; plan D4 and
seams S1/S2. Fixed implementation base: `c5dc4fcaecea5c5301cc37f938b18643edbe7333`.
Candidate: `d5ad5cbdb8bff83b39df860ea211aa3b94308f08`.
Status: accepted; both independent axes and frozen qualification are clear.

## Standards

Independent fixed-slice reviews found recovery scans swallowing access errors,
lock replacement during backup, stale paused-resume writes, acquisition errors
misclassified as contention, and an obsolete failure notice after a successful
retry, plus an archive copy succeeding before active-file removal failed.
Each has a permanent native reproduction and repair. Existing mutation,
work-revision, focus-generation, storage and recovery boundaries are reused.
At d5, Standards independently passes six native cases covering all final
storage/control compositions. Its documentation request is resolved by the
reviewed architecture update, including retained copies and retry limitations.

## Spec

Independent reviews found navigation holds lost across reopen/reload, confirmed
paused resume failing to release a hold, external proposals overwritten by a
stale dialog, denied pause writes escaping the command, missing actual-host
clear coverage and a cached complete status hiding valid branch focus. Native
reproductions cover each finding. Follow-up also found same-revision lifecycle
controls bypassing the work fingerprint and clear detaching before a buffered
archive committed. The complete prior record is now validated for lifecycle
replacement, and clear requires a successful flush before immediate archival.
At d5, Spec independently passes the complete 58-case native matrix and
identifies no remaining concrete implementation or acceptance-coverage finding.
`review-d5ad5cb-native-green.log` retains that independent result.

## Acceptance mapping

| Criterion | Observable evidence |
| --- | --- |
| Every D4 ownership row | Actual Pi loader/session runtime exercises active/paused/blocked/limited reopen and reload; paused confirmation; default/auto-selected new session; explicit-null/missing focus; fork/tree; public focus/unfocus/clear. Native compaction cases inherited from 002 remain in the full suite. |
| Project progress and session authority | A public-created goal has verified task evidence and an independently inspected artifact before navigation. Fork appends null focus and tree persists its hold through compaction/reopen/reload; neither performs unsolicited work. Explicit resume/focus authorizes one checkpoint. Stale resume and recovery callbacks cannot affect a replacement session. |
| Legacy reads and safe records | Legacy goal/focus migration restores disk evidence over historical chat; public refresh reads user-edited objective text with a warm cache. Existing legacy draft/branch tests remain. Truncated/symlinked goals and corrupt snapshots preserve unrelated files; unsafe embedded paths cannot redirect public writes. Missing cached focus is resolved against authoritative records. |
| Mutation failures and conflicts | Native public task mutations cover denied goal/lock access, live-lock contention, ledger-only failure and stale work revisions. Failed writes preserve exact goal/ledger bytes; live contention retains a retryable buffer. Failed public pause/resume/clear writes report diagnostics without success or focus loss. Clear cannot detach behind a locked buffer; a partial archive is explicitly reported and its active record remains authoritative until a successful explicit retry. |
| Public recovery | Health/report are read-only. Cancel changes no durable bytes. Confirmed repair backs up stale locks/snapshot and diagnoses incomplete repairs. File/directory access, backup, snapshot and replacement-session failures retain valid progress. Locks changed before/during copy are not removed. |
| Delegated sessions | Actual child new/fork/reopen/nested loaders expose no parent controls or inherited continuation and preserve parent bytes/usage/evidence. Native 200-task Oracle/auditor transport checks dedicated prompts/tool profiles, no parent projection and Oracle read-only tools. |

`tests/goal-ownership-worker.mjs` creates goals and task proof through public
tools before injecting faults. It binds via the native runtime's rebind callback
after replacement, records outbound requests/results and checks goal files,
ledger, focus entries and independent artifacts. It invokes no skipped hooks.

## Verification and retained attempts

Evidence lives in `~/Data/pi-goal-x/reliability/006/`. All failed attempts and
review reproductions remain alongside passing runs:

- `tree-*`, `fork-*`, `reopen-*`, `reload-*`, `new-*`, `missing-focus-*`:
  navigation authority, durable holds and ownership characterization.
- `recovery-*`, `legacy-refresh-*`, `record-*`: public diagnosis/repair, cache
  refresh, access failures and file/path preservation.
- `storage-*`, `clear-*`, `resume-stale-*`, `review-*.mjs`: failed writes,
  retained retry semantics and independently reproduced review findings.
- `review-fixes-native-final.log`: 55 native ownership/storage/recovery cases.
  `review-fixes-targeted.log`: 151 targeted regression checks. The strengthened
  successful-retry notice check passes with the 17 affected native cases in
  `review-fixes-retry-native.log`; it previously failed in
  `storage-retry-notice-red.log`.
- `clear-control-native.log` and `clear-control-targeted.log`: 21 affected
  native lifecycle cases and166 file/archive/mutation checks after the complete
  record guard and persisted-clear fixes. The three additional native reds and
  greens are `resume-stale-control-confirm-*`, `storage-clear-lock-*` and
  `clear-unlink-failure-*`.
- `child-oracle-audit-native.log`: public 200-task plan, three manual compactions,
  30 executor requests, seven summaries and four independently observed effects,
  with actual isolated Oracle/auditor child transport.

Frozen earlier slices passed 1085,1097,1098,1104,1109,1117 and1127 full checks.
The milestone log corrects an initially premature lint claim for `ba9a4b8`:
its unreachable return failed lint and was removed before the subsequent pass.
The old factory-time test binding produced an invalid nested session ordering;
the corrected native ordering still reproduces stale-context failure on d013.

At d013, type/lint, selfcheck952, context28/provider7, historical NAF gate,
five ranking checks, dry pack and production audit (zero vulnerabilities) pass.
Fresh B1/B2/B5b/B7 match75 historical timing limits with zero regressions.
Frozen781 passes1138 full checks (76files,142.2s), selfcheck952, dry pack,
production audit and fresh75 timing limits with zero regressions.

Final `d5ad5cb` passes **1141/1141 full tests** (76 files,143.4s), type/lint,
discovery/selfcheck952, context28/provider7, historical NAF gate, five ranking
checks and dry pack. Fresh B1/B2/B5b/B7 matches75 unchanged timing limits with
zero regressions. Dependencies are unchanged from the isolated005 installation;
the production audit at781 reports zero vulnerabilities. `review-checks.json`
records commands, exact runtime commits, outputs and these qualifications.

The benchmark checks local synthetic timings against unchanged limits. Public
host evidence uses deterministic provider responses; it does not establish
semantic Qwen acceptance. 007 owns in-run stop/steering races; 008–011 own
budget, blocker/provider recovery and durable completion/archive recovery.
012 owns packed installation/compatibility, 013 the frozen six-run Qwen gate,
and014 adoption preparation. No live package selection is changed.
