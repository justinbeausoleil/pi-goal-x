# Ticket 008 accounting and budget review

Candidate: `40dfcf72bb4a960496485b5fd8843bb8ff7012eb`.
Fixed cumulative base: `35a1093`. G2/G11; native S1/S2.
Both independent reviews are clear. Ticket qualification passes.

| Required outcome | Observable evidence |
| --- | --- |
| Origin accounting, once per response | Public model/guided creation and native custom checkpoints; final pause, abort, focus/unfocus/replacement/clear, unpaid rename faults and later archival, native Pi retry and three compaction paths. Response reports are compared with active/archive records. Repeated-event helper coverage retains the same response object. |
| Exact and overshoot limits | Public task/evidence fixtures cross770/770 and770/760. One persisted transition records770; one reminder includes the remaining current task and completion count. A subsequent write is rejected. Late unfocused charges and delayed refresh payments limit the original owner. |
| Stopped compaction/reopen | Real summarization and session reopen preserve usage, tasks and limited state. Exhausted resume/focus, paused reopen confirmation and paused/blocked scope revisions cannot reactivate work. |
| Budget changes and explicit resume | User-owned JSON metadata raises/removes the budget; refresh preserves stopped state and usage. Only explicit resume allows the next verified work effect. Scope and evidence remain unchanged. |
| Existing accounting meaning | Controlled clocks cover creation, user/agent stops, replacement, completion and summary intervals. Reported executor tokens are separate from real summaries and777-token auditor/Oracle reports. Positive-integer validation remains covered by existing checks. |

Private evidence: `~/Data/pi-goal-x/reliability/008/`.
The milestone log identifies red reproductions, fixture corrections, commands
and intermediate commits. No unsaved in-memory charge is claimed to survive
process termination while storage is unavailable.

Standards independently passes10 native checks at48191cc and clears the final
completion delta; exact commands/results are transcribed in
`review-48191cc-standards-checks.json` (no separate raw process log).
Spec independently passes18 native checks in `review-48191cc-native.log` and
the original completion-clock reproduction at40dfcf7 in
`review-completion-clock-40dfcf7.log`. All three review findings are resolved:
pending archival locator, model-creation clock, and completion's stale usage.

Frozen48191cc passes1275/1275 full tests,953 discovery/selfchecks, type/lint,
28 context fixtures,7 actual SDK payloads, historical NAF, ranking5, production
audit with zero findings and package dry run. Final40dfcf7 adds the registered
completion regression, passes30 related checks and fresh B2/B7's53 unchanged
timing limits with zero agent/network/spawn violations. Its frozen full suite
passes1276/1276 tests across48 suites and76 files in219315ms, zero skipped
(`completion-full.log`); final type/lint and dry pack also pass. Exact commands,
commit identities and results are in `review-checks.json`. Exact package installation,
compatibility, six Qwen runs and adoption remain tickets012–014.
