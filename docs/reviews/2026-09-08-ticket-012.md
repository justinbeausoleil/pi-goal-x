# Package compatibility and launcher review

Scope: tickets 012/012a, G9/G13, native seams S1/S2. Fixed base
`b84ec25761ded42155a2cde316c5c8ab7dcb133d`; implementation review completed
through `b270d47102c47b2b78b7744657265c6898597f01`. Later packaging text only
links to current evidence, avoiding a mutable status inside a frozen artifact.
Ticket 013 owns real-model acceptance; this record does not claim it passed.

## Standards

No material documented violations or actionable heuristic smells. Both the
mounted-widget repair and launcher reuse existing facilities. The classifier
is reused rather than copied, and the runtime has no new dependency or public
surface. Independent native harness and package-surface checks passed.

## Spec

No material findings remain. Review reproduced two launcher failures that the
original shell tests could not detect: legitimate clarification yields prevented
the next user command, and active-goal authorization errors became timeouts.
Both original native reproductions now pass with the expected command sequence
and failure status. A separate transient outage exhausts Pi's immediate retries,
then crosses the quiet window to a real extension-recovery checkpoint and public
pause. Stalled requests, scheduled abort and invalid configuration remain checked.

The earlier widget reproduction showed that navigation callbacks lacked the
mounted component. Both existing factory callers now retain it in the existing
reference. No command was added: upstream and fork each register 16; the original
planning count of 17 was a counting error.

## Verification

Evidence root: `~/Data/pi-goal-x/reliability/012`. The final manifest records the
artifact hash, source commits, commands, versions and exact log locators.

Final manifest: `qualification.json`. Artifact source:
`8e012c7323b8cb21c223432fab0a53b83683fe5e`; artifact in `attempt-04`, SHA256
`e2f0f850bf2d66cc5c042e29c429168a40ad7bb9f6200014138e56e8f94e7aa6`.
Its six fresh native checks pass on Node 22.15.0/24.0.0; rollback-07 restores
78 files exactly and retains the fork-written copy separately. Runtime/package
metadata byte comparisons retain the broader matrix's coverage.

| Check | Result / evidence |
| --- | --- |
| Dependency install | `npm ci --ignore-scripts` passed; unchanged lockfile after `1041183`; `install.log` |
| Type / lint | Passed; `check-final.log`, `lint-final.log` |
| Full suite | 1,352/1,352, 48 suites, 76 files, no failures/skips; `full-9190336.log` |
| Discovery/self-check | 954/954, 71 files; `selfcheck-9190336.log` |
| Actual SDK serial suite | 954/954, 43 suites; `serial-final.log` at `c40848b` |
| Later launcher-only repairs | Native executable checks against the installed artifact on Node 22.15.0/24.0.0, 2/2 each; `attempt-03/harness-node-*-repair.log` |
| Context / SDK provider payloads | 28 fixtures / 7 payloads passed; `context-final.log`, `provider-final.log` |
| Fresh B2/B7 benchmarks | 53 unchanged limits, no regression or guard violation; `final-benchmark.json` |
| Historical NAF CI gate / ranking | 105 after rows against 95 before rows passed; ranking 5/5; `naf-9190336.log`, `ranking-9190336.log` |
| Dependency audits | Production and full development: zero vulnerabilities; `*-audit-final.json` |
| Pack / native compatibility | 68 files; real isolated installation, 17 cases per Node version; `attempt-02/native-matrix.json` |
| Final artifact equivalence | Runtime and package metadata byte comparisons plus fresh exact-artifact native checks, recorded in the final manifest |
| Rollback | Actual upstream install/read, fork migration/public mutation, exact restoration of 78 package/settings/session/project files, then upstream reload; fork-written copy retained separately |

The full suite and serial results remain pinned to their exact commits. Changes
afterward are confined to the launcher, its native tests, and documentation;
the later executable checks cover those repairs against the installed package.
Layered settings, disabled features, delegated children, UI, legacy reads and
fault paths are covered by the full suite and packed lifecycle matrix.

## Preserved failures and limits

Initial widget navigation, removed SDK exports, ignored drain deadlines and
malformed JSON all have retained red logs. Review reproductions are under
`spec-review-9190336`, `spec-review-c40848b` and `spec-review-b270d47`; standards
results have corresponding directories. The packed helper import failure and
repair are retained in `attempt-03/harness-node-*.log`.

Rollback attempts 01–04 exposed fixture comparison/legacy-flag mistakes, not
serialized data loss; all attempts are retained. File restoration assertions
remained byte-exact. The development audit found and repaired a vulnerable
transitive js-yaml version without adding dependencies.

Historical optional model cases are not certified by launcher membership. C16
uses current native compaction settings; its artifact rubric alone cannot prove
a compaction occurred. No rubric assertion was removed or relaxed. The six-run
Qwen gate, live installation and publication are separate; live settings remain
unchanged.

Standards: 0 outstanding findings. Spec: 0 outstanding findings.
