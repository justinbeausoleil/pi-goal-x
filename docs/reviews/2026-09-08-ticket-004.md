# Ticket 004 independent review

Scope: ticket 004, spec G2/G4, plan D1/D2 and retained presentation behavior.
Initial fixed diff: `git diff 7e39724...8adbbba`.
Final follow-up: `git diff 8adbbba...5046e7b`.
Separate read-only Standards and Spec reviewers examined both commits.

## Standards

No documented breaches or actionable heuristic findings. Existing viewport,
structural renderer, mutation service and public revision reader are reused.
The reviewer checked native regression semantics and documentation; it did
not independently rerun the full gates.

## Spec

The initial reviewer passed native detail/accounting and large threshold
compaction, then reproduced two acceptance failures:

- The retained overlay used width to size its scroll viewport, but Pi cropped
  its output to 80% of terminal height. At 80x24, its last seven tasks could
  never be reached. The repair reserves frame, indicator and diagnostic rows
  within the real height limit. A native compositor regression now pages all
  200 tasks and 600 Unicode requirement tokens through both overlay and task
  confirmation, including grow/shrink transitions. No removed shortcut returns.
- Missing-task/non-task-section requests returned before cursor restart
  guidance. Those paths and explicitly empty cursors now share the invalid/stale
  diagnostic. The public worker retains all original cursor rejection cases.

The follow-up independently passed the host compositor worker, all 16 existing
overlay/confirmation tests, and the details/concurrent-accounting worker:
200 nodes, 65 requests, one work effect, successful reopen. Both findings are
resolved; neither reviewer found a new actionable issue at 5046e7b.

Root also found three benchmark clients left on the old task protocol. Effect
assertions first failed (completion still pending or final focus absent), then
passed with public revision reads. Both reviewers cleared these changes.
Historical benchmark artifacts and thresholds remain unchanged.

## Evidence

Final checks at 5046e7b: type check and lint; 1011/1011 full tests across
76 files; discovery plus 932 unit checks; five ranking harness tests; dry
package and zero-vulnerability production audit. All passed. The 24 context
fixtures and six provider payloads passed at unchanged final payload code.
NAF historical gate passed; fresh B2/B7 measurements cover 53 matching rows
with no regression against the existing thresholds, and the exclusion guard
passed. Runtime-token's one-event smoke validates the revised task client only.

`~/Data/pi-goal-x/reliability/004/` retains all red/green logs, including evidence
truncation, page metadata, canonical/empty/wrong-target cursors, hidden UI rows,
draft-proposal navigation, dashboard anchoring, and benchmark client failures.
`review-checks.json` records final commands, outcomes and commits.

Native manual/threshold/overflow cases build 200 tasks through four public
upserts, continue t142 onward across three compactions and a fourth paused
compaction, and preserve three real work artifacts. Long objective/contracts,
pending Oracle advice and auditor rejection remain within the 10,000-character
automatic goal-text bound. Public 4,000-character detail pages reconstruct
full Unicode objective, contracts, task evidence and all 200 task IDs exactly.

Scope/review detail sections retain owners 005/011. Installed-artifact, version
matrix, real-Qwen and adoption checks retain owners 012–014.
