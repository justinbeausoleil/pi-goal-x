# Ticket 001 independent review

Scope: spec G1, ticket 001, plan D1/S1 and lifecycle preservation.
Initial fixed diff: `git diff 4c0f23d...169b790`.
Follow-up: `git diff 4c0f23d...3ac2d4c`; final measurement fix:
`git diff 3ac2d4c...5619d5f`. Separate read-only reviewers examined standards
and specification; the implementation writer resolved findings.

## Standards

Initial review: no documented-standard breaches. Two smell judgments:

- Possible Primitive Obsession: the string `invalid-checkpoint` could match a
  supported persisted goal ID. Resolved with empty identity, which the
  actionable-goal predicate always rejects.
- Possible Duplicated Code: context handler and projection builder both
  reconciled state and assigned execution identity. Resolved by keeping that
  work in the context handler.

Follow-up found one documented-standard breach: decoded semantic measurement
omitted native compaction/branch summary text, weakening complete-request
measurement. A new duplicate-marker probe failed for both summary types. Pi's
native `convertToLlm` now supplies the text; the probes and 24-fixture gate pass.
Final standards review at 5619d5f: all findings resolved, none remaining.

## Spec

Initial review reproduced two defects:

- An incomplete legacy marker containing the active ID still dispatched a
  write. Complete supported marker syntax is now required; the permanent
  actual-host `reject-malformed-prefix` case passes without a work effect.
- Budget exhaustion during a custom-start run bypassed wrap-up instructions.
  Same-goal stopped lifecycle context is now retained. The public `create_goal`
  case with a 200-token budget reports 220 used, 20 over budget, and one wrap-up
  without additional work tools. Its first repair also exposed and corrected
  the inherited clamped-balance display calculation.

Follow-up specification review at 3ac2d4c: both findings resolved, no new
findings within ticket 001. Repeated compaction, aggregate bounds, and other
later-ticket criteria remain pending with their original owners.

## Verification

- `npm ci --ignore-scripts`: passed with Pi 0.85.1 aligned dependencies.
- `npm run check`, `npm run lint`: passed.
- `npm run test:lifecycle`: 12 actual-loader/session/provider cases passed.
- `npm run test:all`, `npm run test:selfcheck`: 973 tests passed; discovery
  includes 71 unit, two integration, and three e2e files.
- `npm run context:gate`: 24 fixtures plus native-summary counting probes pass.
- `npm run context:provider-check`: six actual SDK payloads pass.
- `npm run bench:gate:naf`, `npm pack --dry-run`,
  `npm audit --omit=dev`: passed.
- `uv run --no-project python -B scripts/test-update-ranking.py`: 5 passed.
- Clean upstream fe430b25: locked install, typecheck and all 961 tests passed
  in a detached scratch worktree. No inherited failing test was suppressed.

Exact commands, results, red reproductions and failed repair attempts:
`~/Data/pi-goal-x/reliability/001/`, particularly `review-checks.json` and
`review-summary-*` logs. The development runtime was Node 26.8.1; the declared
Node compatibility matrix and real installed artifact are ticket 012 gates.
No S3 result or live-adoption readiness is claimed by this review.
