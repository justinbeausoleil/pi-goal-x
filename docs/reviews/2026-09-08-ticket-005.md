# Ticket 005 independent review and evidence

Scope: guided lifecycle and retained scope, spec G7/G10, plan D2/D3/D4.
The implementation follows ticket 004's verified runtime at `5046e7b`.
Separate read-only Standards and Spec reviewers examined coherent fixed-base
slices through `dc4d94e`; both final follow-up reviews are clear.

## Standards

Reviews through `dc4d94e` have no remaining material finding. The retained
scope lives in the existing goal record and mutation service; existing dialogs,
work revisions, detail paging, auditor and session lifecycle are reused.
Independent auditor/policy validation at `970330a` passes 26 checks; final
disabled-tracking drafting/core-tool validation passes 67 checks.

Earlier reviews found accounting rejection/reconciliation edges: discarded
owed usage after proposal resolution, interference with a different focused
goal's locked buffer, and duplicate charging after a single-task write. Each
has a permanent red/green regression and a reviewed repair. The single-write
baseline repair at `e0d0cb4` cleared both axes before integration.

## Spec

Earlier findings included stopped-state guidance hidden by the pending-scope
projection, explicit pause not taking effect during a pending task edit, and
buffered usage loss. These were reproduced and repaired before integration.
The existing guidance and ledger behavior remain covered; pending proposals
do not acquire execution or audit authority.

Final review independently passes the real child auditor, paused/blocked/
budget-limited native tweaks and auditor/policy tests. An independent native
active/autoContinue probe at `970330a` also passes: refinement/cancellation
hold work, confirmation dispatches one revised-objective checkpoint, and no
resume event is invented for an already-active goal. Its script and log are
retained in the evidence directory below. The final disabled-task follow-up
passes both native modes (18 requests each) and 67 drafting/core-tool checks.
The reviewer identifies no remaining concrete ticket 005 behavior or coverage gap.

## Acceptance evidence

| Ticket criterion | Observable proof |
| --- | --- |
| Guided affordances in both modes | Native draft worker covers read-only reconnaissance, question/questionnaire, continue/propose/confirm/cancel, selector resume/cancel/replace, auditor enabled/disabled choice and task-disabled confirmation. Startup worker separately proves fully specified guided starts need no synthetic question. |
| Durable branch-local drafts | Native compaction/reopen, two draft branches and cancellation tombstones; selected questionnaire answers restore without cross-branch leakage. Actual host fork inherits discussion but tombstones detached tweak and appends null focus. Dialog cancellation/navigation/focus/work changes invalidate stale answers. |
| Retained scope and lossless reads | Public create/progress/delete/skip/recreate, long Unicode contracts/evidence, complete scope pages of at most 4,000 characters, content-bound cursor rejection and reopen. Legacy first-write migration keeps the work fingerprint stable, including concurrent accounting. |
| Complete human revisions | Native 200-node before/after UI payload; cancel retains all requirements, confirm removes one and revises another. Changed completed title/contract reopens and clears current proof across upsert/replacement/tweak; unaffected proof survives. No-plan tweak preserves the plan. |
| Scope cannot be waived indirectly | Public skip/deletion, AUTO_CONFIRM=1, hidden tasks/contracts and missing evidence remain gated. Headless/model-approval probes cannot authorize scope revision. Removed unresolved IDs expose the recreate-or-revise path. |
| Optional tracking and cancellation | Existing optional/block_completion/parent/lightweight tests remain; native task-disabled structured proposals create no hidden plan. Draft/question cancellation preserves approved state; accepted receipts survive injected ledger failure and reopen with diagnostics. |
| External proposals | Both modes cover objective, goal contract, task contract/title and new contracted-task edits with unchanged numeric revision. Actual cold reopen/refresh/compaction hold automatic work, preserve raw proposal and approved scope, reject ordinary replacement/completion, then bind human confirmation to fresh work revision. |
| Tweak lifecycle matrix | Both modes exercise active/manual, paused, blocked and exhausted budget_limited. Refinement/cancel append no lifecycle events or checkpoints. Paused/blocked clear metadata and resume once; active stays active; limited stays stopped. Exhausted resume is rejected with actionable budget/refresh guidance. |

The isolated native auditor receives full retained objective/contracts and
removed-task evidence after public deletion, hidden settings, compaction and
reopen. The fixture independently inspects its artifact and intercepts the
actual child request; no parent goal tools or executor projection leak into it.
Its controlled rejection proves transport/context integrity. Semantic review
outcomes and real-model acceptance remain owned by 011/013.

## Verification and artifacts

`~/Data/pi-goal-x/reliability/005/` retains all red/green attempts and detailed
milestones preserve their interpretation. In particular:

- `draft-*.log`, `active-*.log`: cancellation, stale dialogs, branch restoration,
  active drafting and settings checks. The original unapproved cancellation
  loop dispatched 61 checkpoints; the repair dispatches zero until explicit resume.
- `scope-*.log`: retained-scope migration, removal/evidence/recreation, paged
  detail, 200-node tweak, ledger fault and external-proposal cases. All failed
  accounting/review attempts remain alongside their passing follow-ups.
- `scope-audit-native-*.log`: actual child request and full Unicode proof,
  including the initial fixture failures. The 200-node long manual/advice
  compaction passes 30 executor requests, seven summaries and four effects.
- `tweak-lifecycle-*.log`: all eight lifecycle cases, including the exhausted
  resume red; `tweak-budget-policy-{red,green}.log` retains its validator proof.
- `review-active-auto-970330a.{mjs,log}`: independent active/automatic probe.
- `draft-affordances-*.log` and `draft-disabled-derived-unit-{red,green}.log`:
  normal question/selector choices and the repaired hidden derived-task defect.

Frozen `e0d0cb4` passes 1069/1069 full tests; frozen `970330a` passes 1081/1081
across 76 files. Type/lint, discovery/self-check, context 28/provider 7, dry pack,
production audit (zero vulnerabilities), NAF historical gate and five ranking
tests pass. Fresh B2/B7 measurements at the external-scope subset cover 53
matched timing limits with zero violations; these are local synthetic results,
not model-performance claims. Existing benchmark thresholds are unchanged.

Final `dc4d94e` qualification after a fresh isolated `npm ci --ignore-scripts`
passes type/lint and 1083/1083 full tests (76 files, 113.7s). Discovery/self-check
passes with 952 unit checks. `review-checks.json` records the commands/results.

Remaining owners: 006 general ownership/recovery; 007 in-run stops; 008 full
accounting/budget refresh; 011 durable review outcomes and archive recovery;
012 actual packed installation/compatibility; 013 frozen Qwen matrix; 014
adoption preparation. Live selection remains unchanged.
