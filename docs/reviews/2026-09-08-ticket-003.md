# Ticket 003 independent review

Scope: ticket 003, spec G3, plan D2/S1/S2 and preserved task semantics.
Initial fixed diff: `git diff 9a16d35...3eb7feb`.
Final follow-up: `git diff 9a16d35...4b900f7`.
Separate read-only Standards and Spec reviewers examined the slice and repairs.

## Standards

No documented-standard breaches or substantive smell findings in the initial
review. The implementation reuses GoalService, explicitly passes revisions in
test clients, retains raw missing/stale-input tests, and proves capacity through
actual Pi sessions and public tools. Architecture and release notes match D2.

The reviewer separately cleared cc6714f's shared descendant-focus repair and
4b900f7's locked accounting rebase. No remaining Standards findings.

## Spec

The reviewer independently passed the native 200-node/reopen proof, then found
a concurrency defect: a second session's usage-only save between a successful
task result and turn flush caused the first session's task progress to revert.
The old numeric revision guard rejected the complete buffered transaction.

The shared flush now compares the fresh record with the original normalized
base under the lock, allowing only usage, updatedAt and storage revision to
differ. It adds the local usage delta before writing and appending task events.
Reconciliation before the first buffered write refreshes the base to avoid
charging adopted remote usage twice. Work, pause and budget conflicts still
reject without overwriting the other writer or appending speculative events.

The follow-up independently ran the native concurrent-accounting worker at
4b900f7: 200 nodes, 33 requests, exact tokens/seconds, single completion/start
history events, and preserved evidence through reopen. The reviewer also
cleared the current-descendant repair. No remaining Spec findings for 003.

## Verification

- `npm run check`, `npm run lint`: passed.
- `npm run test:all`: 999/999 across 76 discovered files, including 28 native
  lifecycle/compaction/large-plan cases.
- `npm run test:selfcheck`: discovery checks and 925 unit tests passed.
- Targeted storage/transaction/progress checks: 41 passed.
- `npm run context:gate`: 24 fixtures passed; D2 payload changes are documented
  in the existing baseline rationale without raising the 10,000-character cap.
- `npm run context:provider-check`: six SDK payloads passed sequentially after
  the context gate. Payload code is unchanged by subsequent task/accounting fixes.
- `npm run bench:gate:naf`, `npm pack --dry-run --json`, and
  `npm audit --omit=dev`: passed; production vulnerabilities: zero.

Exact commands/results: `~/Data/pi-goal-x/reliability/003/review-checks.json`.
That directory retains public-protocol, stale revision, structural scope, UI
race, descendant-focus and accounting red/green logs. Failed fixture/type
attempts and old-client migration failures remain recorded in the milestone
log. One manual targeted run used the benchmark adapter; its Unicode ledger
failure does not occur with the correct test adapter/native SDK.

Ticket 004 owns full 200-node detail/UI/compaction proof. Ticket 005 replaces
interim contract-removal/completed-edit rejection with human-confirmed scope
revision and retained obligations. Package installation, compatibility, real
Qwen acceptance and adoption preparation retain their later ticket owners.
