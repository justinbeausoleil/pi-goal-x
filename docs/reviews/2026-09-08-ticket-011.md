# Ticket011 completion integrity review

Candidate: `6a76032863f53319a0d2738dd6ef8fa71438aa9a`.
Fixed cumulative base: `8f2745fc2c34c5d52c4c6427273058d31cef6ecb`.
G6/G7, with G11 accounting preservation; native S1/S2. Both independent axes
pass, and full ticket qualification passes.

| Required outcome | Observable evidence |
| --- | --- |
| Completion gates | Public pending required tasks block completion even with task tools hidden. Optional uncontracted planning may be skipped with a reason. Retained scope and missing evidence remain gates. A real auditor reads a wrong artifact and rejects the executor's false claim. |
| Durable review | All review outcomes and three bypass origins persist in authoritative metadata. Rejection survives three real compactions and reopen with failed ledger writes; full review retrieval remains lossless across4000-character pages. Cancellation/malformed ledger history survives reopen and later approval. |
| Honest result | Actual provider errors differ from malformed/disapproved verdicts. Audited and audit-skipped labels reach the widget and saved record. Completion write faults produce no success message/card; the actual audit still remains diagnostic history. Stale focus/work cannot commit completion. |
| Deferred archival | Tool result observes the complete active record before archival. Write/unlink faults and abrupt process exit leave discoverable complete records outside the open pool. Confirmed recovery backs up exact bytes and archives without another executor/auditor turn. File races, backup failure and stale-session confirmation preserve user content. Advancing-clock retry retains one archive. |
| Lifecycle and accounting | Active, paused and budget-limited completion work when evidence permits; blocked requires resume. Cancellation preserves pause. Final executor usage after Escape bypass remains770/770 tokens in the archive. |

At079398d, spec review found the hidden-task gate bypass, incomplete full review
history and missing late usage. Standards found unreported recovery-ledger
failure outside GoalService. Root full qualification additionally found active
cancellation wording, lost approval diagnostics after commit failure and
duplicate archive copies. All are repaired at6a76032; no limits/assertions were
weakened to accept a failure. The existing stop test now explicitly requires a
cancelled event for Escape/continue while still rejecting late child approval.

Spec independently passed9 follow-up scenarios across10 native processes,
including crash/reopen. Standards independently passed4 repair scenarios and
verified the source pin before/after. No material findings or heuristic smells
remain. Raw commands, SHA/environment and prior failures are in
`~/Data/pi-goal-x/reliability/011/`, including
`review-repairs-6a76032-native.log`, `review-crash-6a76032-native.log`, and
`review-6a76032-standards-checks.json`.

Frozen qualification:1349/1349 tests across48 suites/76 files, zero skips or
failures,263291ms;953 unit discovery/selfchecks. Installation with scripts
disabled, type/lint,28 context fixtures,7 real SDK payloads,53 unchanged fresh
B2/B7 limits, historical NAF,5 ranking checks and dry pack pass. Dry pack has
65 files/270690 bytes. Production dependency audit is clear; full development
audit records one high ESLint transitive js-yaml advisory for012. See the exact
commands and outcomes in `review-checks.json`. Package/Node compatibility,
six-run Qwen acceptance and adoption preparation remain012–014.
