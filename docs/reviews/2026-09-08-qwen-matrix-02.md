# Matrix 02: failed frozen Qwen acceptance

**Result: FAIL, 0/6. G8 and adoption remain blocked.** Every scheduled slot ran
once; no attempt was restarted and no acceptance limit changed. Matrix01 was
superseded before any model attempt, as its retained preparation record states.

Driver/fixture source: `e6302fce600c1016acf83210b44124699a895f65`.
Artifact source: `8e012c7323b8cb21c223432fab0a53b83683fe5e`,
`@justinbeausoleil/pi-goal-x@0.31.2-reliability.1`. Tarball SHA256:
`e2f0f850bf2d66cc5c042e29c429168a40ad7bb9f6200014138e56e8f94e7aa6`.

Evidence: `~/Data/pi-goal-x/reliability/013/matrix-02`. The immutable
`matrix.json` freezes configuration and independent answers; `commands.json`
records the six commands. `results.json` indexes each original result, native
events, responses, usage, transitions and watchdog exit. Private runtime data
remains outside Git.

## Scheduled outcomes

| Run | Qwen | Thinking | Seed | Elapsed seconds | Executor responses | Compactions | Failure |
| --- | --- | --- | --- | ---: | ---: | --- | --- |
| 1 | 3.6-35B-A3B | off | 101 | 572.511 | 50 | none | Missing exact milestone IDs |
| 2 | 3.6-35B-A3B | low | 102 | 396.692 | 18 | none | Missing exact milestone IDs |
| 3 | 3.6-35B-A3B | low | 103 | 578.460 | 22 | none | Missing exact milestone IDs |
| 4 | 3.8-27B | off | 101 | 1194.145 | 19 | manual, overflow | Overflow instead of threshold |
| 5 | 3.8-27B | low | 102 | 1800.065 | 11 | none | 30-minute timeout |
| 6 | 3.8-27B | low | 103 | 1800.072 | 15 | manual | 30-minute timeout |

All runs retained context65536, maximum output8192, temperature0.2, top-p0.95,
Pi0.85.1 and Node26.8.1. Limits remained60 executor responses,30 minutes including
audit/compaction/checks, and two extension recovery attempts. No extension
recovery was used. Configuration contains exact model/provider and compatibility
settings without copying the credential command.

## Exposed usage

Values are provider-reported input/output tokens, not estimates of hidden usage.

| Run | Executor input / output | Summary input / output | Auditor input / output |
| --- | ---: | ---: | ---: |
| 1 | 576661 / 13342 | none | 111558 / 2536 |
| 2 | 218511 / 11597 | none | 124710 / 4027 |
| 3 | 302018 / 18114 | none | 113777 / 4021 |
| 4 | 186171 / 5032 | 14379 / 3208 | none |
| 5 | 115791 / 15641 | none | none |
| 6 | 139103 / 11959 | 13311 / 5307 | none |

Cache-read, cache-write and separate reasoning categories were0 wherever
reported; that does not establish absent reasoning. Aborted responses preserve
only the usage actually exposed by the provider. Full response records remain
in each run directory.

## Findings and limits

- Runs01–03 created prefixed task IDs instead of the six exact required IDs.
  They completed their own six tasks and received independent auditor approval,
  but never crossed the required milestone boundaries. Read-only post-run checks
  matched the original three JSON outputs. Fresh-input verification and executor
  tests were not rerun as acceptance; the outcomes remain FAIL.
- Run04 crossed manual compaction, then the frozen256200-character pressure
  raised Pi's estimate to75013. Native output clamping sent max_tokens1; the
  length response triggered overflow. The required threshold path did not fire.
- Run05 repeated identical inspection-command and thinking hashes in responses
  5–10, completed no milestone, and timed out. Cause is not established by those
  hashes. Run06 completed parse and one manual compaction, then timed out before
  the remaining required boundaries. Neither reached final artifact/audit checks.
- The driver's custom system prompt bypassed Pi's registered tool guidance.
  This configuration defect is proven from actual requests; its contribution
  to the model failures is not. Reviewed6170a57 removes that override. Reviewed
  53cb5f1 replaces fixed pressure with a frozen rule targeting51200 native
  estimated tokens; native rehearsals preserve the8192 output allowance. The
  CSV fixture, exact milestones, independent outputs and6/6 gate stay unchanged.
- Replacement qualification overlapped parts of runs05/06. Its three longest
  deterministic cases timed out, and the prior qualified artifact reproduced
  the same timeout under that load. This does not establish that contention
  caused model failures. Timing provenance is in
  Data012/reliability-02/model-overlap.json. Finish qualification with model
  execution idle, and finish all package checks before the next real matrix.

Cumulative review also repaired auditor-toggle reporting and paused-goal
cancellation. Those runtime changes require a new qualified tarball. At matrix02 completion, the
replacement candidate e47f475 / reliability.2 still awaited full qualification.
Its subsequent model evaluation must be a new full matrix, with this failed
matrix retained. No live package selection or publication occurred.
