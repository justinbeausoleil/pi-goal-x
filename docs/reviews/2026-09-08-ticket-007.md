# Ticket 007 independent review and evidence

Scope: G5, plan D4, native seams S1/S2. Fixed base: `7680b30`.
Candidate: follow-up to `0601fa9e2c191b6d097178754b31b19f5ffe0d70`.
Status: cumulative findings repaired; pending independent re-review and frozen qualification.

## Standards

Independent review reproduced a late abort pausing a newly resumed instance of
the same goal. The run now retains the existing focus-generation token as well
as its goal ID. Shared cancellation covers pause, focus switch, clear and
unfocus. The immediate native abort listener protects pending dialogs/reviews;
Oracle uses its existing cancellation parameter. Fixed-slice reviews through
0751fdc are clear. A separate native coverage review verified Pi's sequential
mutation-tool contract and the serialized agent-pause cases below.
Final cumulative review at0601fa9 found successful direct replacement did not
cancel old goal work. The shared replaceGoal boundary now cancels the previous
run after creation succeeds. Native regular/ordered replacement proofs retain
already-dispatched effects, abort the old process and run the new checkpoint;
ordinary work begun under a paused goal still finishes.

## Spec

Independent reviews reproduced clear cancelling unrelated paused-goal work,
an ordinary-work test dispatching its supposed later write too early, and
queued steering failing to prevent automatic work unless accompanied by pause.
Cancellation now checks work ownership. The corrected ordinary test asserts
the later artifact is absent at the control and dispatches it in the next
response. Standalone steer/followUp cases enforce the pending-user gate.
Spec independently passes all73 native cases at0751fdc; final preservation
coverage adds12 cases without changing the runtime.
Cumulative review also found immediate user resume after agent pause could
authorize a later response in the old run. The existing focused-record setter
now invalidates the generation when an active goal becomes stopped. A native
turn_end observer resumes after the pause, and checks that only the subsequent
new checkpoint can write. An earlier generic turn-stop hook wrongly invalidated
task confirmation too; startup failed, so that hook was removed before review.

## Acceptance mapping

| Criterion | Native observable evidence |
| --- | --- |
| Stops at scheduled, response and dispatched boundaries | Pause, Esc, abort, unfocus, focus switch and confirmed clear prevent the next work effect. Running bash retains its first effect and loses its later effect after cancellation. An active selected successor and same-goal immediate resume start their own checkpoints. |
| Task and child-review races | Eight controls against each task dialog, actual HTTP auditor and actual HTTP Oracle verify stale returns cannot change/complete/arm the stopped goal. The original raw-abort task-confirmation red and unwired Oracle signal red remain. HTTP close is observed before claiming cancellation. |
| Agent pause | A real checkpoint emits pause with reason/suggestion, schedules no successor, and preserves fresh ordinary work. Three native batches await task-dialog/audit/Oracle outcomes before agent pause dispatches, then prevent a trailing write. All goal mutation tools are sequential in Pi; the earlier nonterminal result legitimately precedes the later pause. No concurrent same-host agent mutation is fabricated. |
| Generation and queued authority | A checkpoint needs a current single-use runtime UUID/sequence receipt. Replay after resume, reload or reopen cannot regain authority. Old nextTurn attachments cannot replace a fresh user trigger; later host follow-ups remain independently checked. A rejected old delivery cannot erase an explicitly scheduled successor. |
| Pending user steering | Actual autonomous checkpoints race user steer/followUp both with and without separate stop commands. Pending input prevents new goal work; Pi delivers the user's next request. Custom host follow-up and nextTurn cases observe real queue delivery instead of treating hasPendingMessages as exhaustive. |
| Cancellation, eligible resume and stale callbacks | Inherited006 native clear-cancel checks byte-for-byte durable no-op; stale clear/resume/session/tree confirmations remain in the full suite. Pending task cancellation preserves task storage; raw abort invalidates before the confirmation returns. Active successor and same-goal resume proofs cover old callbacks settling afterward. |
| Ordinary work and Esc/inspection preservation | Paused ordinary tools survive pause/unfocus/switch/clear, including a later response's write. Fresh ordinary writes work after every stop. Stale checkpoint runs allow get_goal and block write. Dashboard Escape collapses before a second Escape pauses. Task-dialog Escape cancels the dialog; audit Escape cancels only the review and honors Continue. |

`tests/goal-stop-worker.mjs` uses the actual loader, AgentSessionRuntime, public
tools/commands and host queues. It creates its goal/task records publicly,
observes requests, tool starts/results, files and child HTTP, and invokes no
skipped lifecycle hook. The85 cases are registered in the existing integration
suite at0601fa9; the follow-up adds eight replacement/resume cases.
Independently authorized sessions retain D4's existing shared-storage
semantics; this ticket adds no distributed execution lease.

## Verification and retained attempts

Private evidence: `~/Data/pi-goal-x/reliability/007/`. The milestone log and
`review-checks.json` record commands and exact qualification results.

- Native reds: response/dispatched pause, switch and clear; same-goal immediate
  resume; replay after resume; old nextTurn/follow-up delivery; standalone
  steering; task confirmation after raw abort; Oracle transport cancellation.
- Review probes: `review-*.mjs` and their logs retain independent reproductions
  and both original ordinary-work baseline and repaired outcomes.
- Fixture corrections retain their failures: early idle detection; an active
  successor's output supplied to the wrong run; concurrent ordinary batch
  tools; provider abort-contract handling; HTTP close asserted too early;
  relative storage paths; one checkpoint incorrectly equated with one response.
- Frozen80e9796 passes1161 full checks; 2fec3f7 passes1188; 0751fdc passes1214.
  At0751fdc, type/lint, discovery952,122 targeted checks, historical NAF,
  ranking5, dry pack and fresh B2/B7 (53 timing limits, zero violations) pass.
  Context28/provider7 pass at2fec3f7. Frozen0601fa9 passes1226/1226 full tests,
  type/lint, context28/provider7, dry pack and production audit (zero findings).
  Re-review and qualification of the lifecycle follow-up are pending.

The actual queued user Escape path cancels the timer. A bare idle SDK abort
emits no extension stop event; its characterization is retained rather than
manufacturing an event the host does not provide. Active native abort and
dialog/review cancellation are separately exercised. Already-dispatched
effects are not rolled back. The provider fixtures are deterministic; these
checks do not establish Qwen acceptance. Tickets008–014 retain accounting,
blocker/provider/completion, exact package, model and adoption gates. No live
package selection has changed.
